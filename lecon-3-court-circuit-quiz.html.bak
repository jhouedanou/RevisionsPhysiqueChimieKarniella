<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz - Court-Circuit et Protection</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            display: flex;
            flex-direction: column;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            padding: 0;
        }

        header {
            background: rgba(0, 0, 0, 0.9);
            padding: 20px;
            text-align: center;
            border-bottom: 3px solid #e74c3c;
            position: relative;
        }

        .btn-back {
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
            padding: 10px 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 500;
            text-decoration: none;
            transition: all 0.3s ease;
            font-size: 14px;
            display: inline-block;
        }

        .btn-back:hover {
            transform: translateY(-50%) translateX(-5px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }

        header h1 {
            color: white;
            font-size: 32px;
            margin-bottom: 5px;
        }

        header p {
            color: #bbb;
            font-size: 14px;
        }

        .content {
            flex: 1;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            overflow-y: auto;
        }

        .container {
            background: white;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 30px 80px rgba(0, 0, 0, 0.4);
            max-width: 900px;
            width: 100%;
        }

        h2 {
            text-align: center;
            color: #e74c3c;
            margin-bottom: 10px;
            font-size: 28px;
            border-bottom: 3px solid #e74c3c;
            padding-bottom: 15px;
        }

        .subtitle {
            text-align: center;
            color: #666;
            margin-bottom: 30px;
            font-size: 14px;
        }

        .quiz-section {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .quiz-question {
            background: white;
            border: 2px solid #e74c3c;
            border-radius: 8px;
            padding: 20px;
            transition: all 0.3s ease;
            display: block;
        }

        .quiz-question:hover {
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.2);
            border-color: #c0392b;
        }

        .question-number {
            display: inline-block;
            background: #e74c3c;
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            text-align: center;
            line-height: 30px;
            font-weight: bold;
            margin-right: 10px;
            font-size: 14px;
        }

        .question-text {
            font-weight: 600;
            color: #333;
            margin: 10px 0;
            font-size: 15px;
            display: block;
        }

        .quiz-options {
            margin: 15px 0;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .quiz-option {
            padding: 12px;
            border: 2px solid #e74c3c;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.2s ease;
            background: #f9f9f9;
            font-size: 14px;
            display: flex;
            align-items: center;
        }

        .quiz-option:hover {
            background: #ffe8e8;
            border-color: #c0392b;
        }

        .quiz-option input[type="radio"] {
            margin-right: 10px;
            cursor: pointer;
            accent-color: #e74c3c;
        }

        .quiz-option label {
            cursor: pointer;
            margin: 0;
            flex: 1;
            font-size: 14px;
        }

        .quiz-feedback {
            font-size: 13px;
            margin-top: 10px;
            padding: 10px;
            border-radius: 4px;
            font-weight: 500;
        }

        .quiz-button {
            background: #e74c3c;
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            cursor: pointer;
            font-weight: 600;
            transition: all 0.3s ease;
            font-size: 16px;
            margin-top: 20px;
            width: 100%;
        }

        .quiz-button:hover {
            background: #c0392b;
            transform: scale(1.02);
            box-shadow: 0 5px 15px rgba(231, 76, 60, 0.4);
        }

        .quiz-button:active {
            transform: scale(0.98);
        }

        .quiz-result {
            background: linear-gradient(135deg, #e74c3c 0%, #c0392b 100%);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            margin-top: 20px;
            font-size: 16px;
            font-weight: bold;
        }

        @media (max-width: 768px) {
            header h1 {
                font-size: 24px;
            }

            .container {
                padding: 20px;
            }

            h2 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <header>
        <a href="lecon-3-court-circuit.html" class="btn-back">← Retour</a>
        <h1>⚠️ Quiz - Court-Circuit et Protection</h1>
        <p>Testez vos connaissances sur les courts-circuits et les protections</p>
    </header>

    <div class="content">
        <div class="container">
            <section class="quiz-section">
                <h2>📝 Quiz - Leçon 3: Court-Circuit et Protection</h2>
                <p class="subtitle">8 questions pour évaluer votre compréhension</p>

                <!-- Question 1 -->
                <div class="quiz-question">
                    <span class="question-number">1</span>
                    <span class="question-text">Qu'est-ce qu'un court-circuit?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q1" value="a"> Une connexion directe entre + et − avec très peu de résistance
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q1" value="b"> Un circuit qui manque de puissance
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q1" value="c"> Un fil qui est trop court
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q1" value="d"> Une prise avec peu d'amperage
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q1"></div>
                </div>

                <!-- Question 2 -->
                <div class="quiz-question">
                    <span class="question-number">2</span>
                    <span class="question-text">Quel est le principal danger d'un court-circuit?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q2" value="a"> Cela consomme moins d'énergie
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q2" value="b"> Courant très intense qui génère beaucoup de chaleur
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q2" value="c"> Cela augmente la tension
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q2" value="d"> Cela éteint tous les appareils
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q2"></div>
                </div>

                <!-- Question 3 -->
                <div class="quiz-question">
                    <span class="question-number">3</span>
                    <span class="question-text">Qu'est-ce qui peut causer un court-circuit?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q3" value="a"> Des fils dénudés qui se touchent
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q3" value="b"> Une prise humide
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q3" value="c"> Une surcharge d'appareils
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q3" value="d"> Tous les éléments ci-dessus
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q3"></div>
                </div>

                <!-- Question 4 -->
                <div class="quiz-question">
                    <span class="question-number">4</span>
                    <span class="question-text">Quel appareil coupe automatiquement le circuit en cas de surcharge?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q4" value="a"> Un fusible ou un disjoncteur
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q4" value="b"> Un transformateur
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q4" value="c"> Une multiprise
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q4" value="d"> Un compteur électrique
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q4"></div>
                </div>

                <!-- Question 5 -->
                <div class="quiz-question">
                    <span class="question-number">5</span>
                    <span class="question-text">Que faire en cas de court-circuit soupçonné?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q5" value="a"> Essayer de réparer soi-même avec un tournevis
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q5" value="b"> Couper l'électricité et appeler un électricien
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q5" value="c"> Continuer à utiliser le circuit normalement
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q5" value="d"> Brancher plus d'appareils pour tester
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q5"></div>
                </div>

                <!-- Question 6 -->
                <div class="quiz-question">
                    <span class="question-number">6</span>
                    <span class="question-text">Quel est un danger DIRECT d'un court-circuit?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q6" value="a"> Génération d'une chaleur extrême pouvant causer un incendie
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q6" value="b"> La lampe s'éteint complètement
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q6" value="c"> Le circuit devient plus efficace
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q6" value="d"> La tension diminue légèrement
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q6"></div>
                </div>

                <!-- Question 7 -->
                <div class="quiz-question">
                    <span class="question-number">7</span>
                    <span class="question-text">Comment un court-circuit peut-il se créer par des fils dénudés?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q7" value="a"> Les deux fils positif et négatif entrent en contact direct
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q7" value="b"> Les fils perdent leur isolation naturellement avec le temps
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q7" value="c"> Le circuit manque de terre
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q7" value="d"> La tension devient trop élevée
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q7"></div>
                </div>

                <!-- Question 8 -->
                <div class="quiz-question">
                    <span class="question-number">8</span>
                    <span class="question-text">Quelle est la différence principale entre un fusible et un disjoncteur moderne?</span>
                    <div class="quiz-options">
                        <label class="quiz-option">
                            <input type="radio" name="q8" value="a"> Le fusible se détériore et doit être remplacé; le disjoncteur peut être réarmé
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q8" value="b"> Le disjoncteur est plus ancien que le fusible
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q8" value="c"> Le fusible fonctionne plus rapidement
                        </label>
                        <label class="quiz-option">
                            <input type="radio" name="q8" value="d"> Le disjoncteur ne protège que les appareils de cuisine
                        </label>
                    </div>
                    <div class="quiz-feedback" id="feedback-q8"></div>
                </div>

                <!-- Submit Button -->
                <button class="quiz-button" onclick="submitQuiz()">Vérifier les Réponses</button>
                <div id="quiz-results" class="quiz-result" style="display: none;"></div>
            </section>
        </div>
    </div>

    <script>
        // Réponses correctes du quiz
        const quizAnswers = {
            q1: 'a', // Connexion directe entre + et −
            q2: 'b', // Courant très intense qui génère de la chaleur
            q3: 'd', // Tous les éléments ci-dessus
            q4: 'a', // Fusible ou disjoncteur
            q5: 'b', // Couper l'électricité et appeler un électricien
            q6: 'a', // Génération d'une chaleur extrême pouvant causer un incendie
            q7: 'a', // Les deux fils positif et négatif entrent en contact direct
            q8: 'a'  // Le fusible se détériore et doit être remplacé; le disjoncteur peut être réarmé
        };

        // Fonction pour vérifier le quiz
        function submitQuiz() {
            let score = 0;
            let totalQuestions = Object.keys(quizAnswers).length;

            // Vérifier chaque réponse
            for (let i = 1; i <= totalQuestions; i++) {
                const questionName = 'q' + i;
                const selectedAnswer = document.querySelector(`input[name="${questionName}"]:checked`);
                const feedbackElement = document.getElementById(`feedback-${questionName}`);

                if (!selectedAnswer) {
                    if (feedbackElement) {
                        feedbackElement.innerHTML = '<span style="color: #ff6f00; font-weight: bold;">⚠️ Réponse manquante</span>';
                    }
                    continue;
                }

                const isCorrect = selectedAnswer.value === quizAnswers[questionName];

                if (isCorrect) {
                    score++;
                    if (feedbackElement) {
                        feedbackElement.innerHTML = '<span style="color: #27ae60; font-weight: bold;">✅ Correct!</span>';
                    }
                } else {
                    if (feedbackElement) {
                        feedbackElement.innerHTML = '<span style="color: #e74c3c; font-weight: bold;">❌ Incorrect</span>';
                    }
                }
            }

            // Afficher les résultats
            const resultsDiv = document.getElementById('quiz-results');
            const percentage = Math.round((score / totalQuestions) * 100);

            let resultMessage = `
                <strong>Résultat: ${score}/${totalQuestions} (${percentage}%)</strong><br>
            `;

            if (percentage >= 80) {
                resultMessage += '<span style="color: #27ae60;">🎉 Excellent! Vous maîtrisez bien ce sujet.</span>';
            } else if (percentage >= 60) {
                resultMessage += '<span style="color: #f39c12;">👍 Bon travail! Continuez à réviser.</span>';
            } else {
                resultMessage += '<span style="color: #e74c3c;">📚 À relire! Revisitez le contenu.</span>';
            }

            resultsDiv.innerHTML = resultMessage;
            resultsDiv.style.display = 'block';

            // Scroll vers les résultats
            resultsDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
        }
    </script>
</body>
</html>
